apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "antares.fullname" . }}-test-connection"
  labels:
    {{- include "antares.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "antares.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
